package ua.nure.mikisha.ltd.tests;

/*import org.junit.Assert;
import org.juint.Test;

import static org.juint.Assert.*;

import ua.nure.mikisha.SummaryTask4.db.UserManager;
import ua.nure.mikisha.SummaryTask4.db.entity.User;
public class EntityTest{
	@Test
	public void UserTest() {
		User user = new User();
		user.setLogin("testeruser");
		user.setPassword("password");
		User user2 = new User();
		user2.setId(user.getId());
		user2.setLogin(user.getLogin());
		AssertNotNull(user.getId());
		AssetNotNull(user.getLogin());
		AssertEquals (user,user2);
	}
}*/